---
- name: "transmission | {{ ansible_facts['distribution'] }} | Install transmission"
  ansible.builtin.apt:
    name: transmission
    state: present
    update_cache: true
    cache_valid_time: 3600
  become: true
