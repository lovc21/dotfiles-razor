---
- name: "xournalpp | {{ ansible_facts['distribution'] }} | Install xournalpp"
  ansible.builtin.apt:
    name: xournalpp
    state: present
    update_cache: true
    cache_valid_time: 3600
  become: true
