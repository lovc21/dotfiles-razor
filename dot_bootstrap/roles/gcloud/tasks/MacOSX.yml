---
- name: "gcloud | MacOSX | Install google-cloud-sdk"
  community.general.homebrew_cask:
    name: google-cloud-sdk
    state: present
  become: false
  become_user: "{{ ansible_facts['user_id'] }}"
