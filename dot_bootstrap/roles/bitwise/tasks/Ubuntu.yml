---
- name: "bitwise | {{ ansible_facts['distribution'] }} | Install bitwise"
  ansible.builtin.apt:
    name: bitwise
    state: present
    update_cache: true
    cache_valid_time: 3600
  become: true
