---
- name: "pay-respects | {{ ansible_facts['distribution'] }} | Install pay-respects via cargo"
  ansible.builtin.command: cargo install pay-respects
  args:
    creates: "{{ host_user_home }}/.cargo/bin/pay-respects"
  become: false
