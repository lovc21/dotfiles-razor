---
- name: "pay-respects | {{ ansible_distribution }} | Install pay-respects via cargo"
  ansible.builtin.command: cargo install pay-respects
  args:
    creates: "{{ ansible_env.HOME }}/.cargo/bin/pay-respects"
  become: false
