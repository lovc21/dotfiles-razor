---
- name: "gnome-boxes | {{ ansible_facts['distribution'] }} | Install gnome-boxes"
  ansible.builtin.apt:
    name: gnome-boxes
    state: present
    update_cache: true
    cache_valid_time: 3600
  become: true
